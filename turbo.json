{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["build/**", "dist/**"]
    },
    "//#build": {},
    "//#test": {
      "dependsOn": ["build"],
      "outputs": []
    }
  }
}
