import { initializeUTMParamsCapture, getUTMParamsCaptureState } from './'

describe('initializeUTMParamsCapture', () => {
  it.todo('should set allowed list of UTM params as cookies', () => {})
})

describe('getUTMParamsCaptureState', () => {
  it.todo('should return an object of UTM params from cookies', () => {})
})
